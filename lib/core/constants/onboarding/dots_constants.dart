import 'package:flutter/material.dart';

class DotsConstants {
  DotsConstants();

  final BorderRadius dotsBorderCircular = BorderRadius.circular(25);
  final BorderRadius buttonBorderCircular = BorderRadius.circular(8);
  final Size dotSize = const Size(20, 10);
  final EdgeInsets dotSpacing = const EdgeInsets.symmetric(horizontal: 3);
  final Size dotSquare = const Size.square(10);
}
