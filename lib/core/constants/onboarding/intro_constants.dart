import 'package:flutter/material.dart';

class IntroConstants {
  const IntroConstants._();

  static BorderRadius dotsBorderCircular = BorderRadius.circular(25);
  static BorderRadius buttonBorderCircular = BorderRadius.circular(8);
  static const Size dotSize = Size(20, 10);
  static const EdgeInsets dotSpacing = EdgeInsets.symmetric(horizontal: 3);
  static const Size dotSquare = Size.square(10);
}
