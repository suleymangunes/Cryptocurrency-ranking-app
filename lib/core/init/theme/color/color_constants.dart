import 'package:flutter/material.dart';

class ColorConstantsCommon {
  const ColorConstantsCommon._();

  // error and price down
  static const Color scarlet = Color.fromRGBO(255, 20, 20, 1);
  // succes and price up
  static const Color malachite = Color.fromRGBO(20, 255, 67, 1);
  // unselected and not found
  static const Color dustyGrey = Color.fromRGBO(150, 150, 150, 1);
}
